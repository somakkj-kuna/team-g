# PROCESS_LOG — 작업 기록 (과정 70점의 핵심 근거)

> 표준 헤더(CLAUDE.md 등)를 로드했다면 에이전트가 알아서 채워 줍니다. 비면 직접 채우세요.
> 원칙: **실제로 시킨 프롬프트를 그대로 인용**할 것. 요약만 있으면 점수가 깎입니다.

## 작성자 정보 (개인별 로그 — 본인 것만)
- 팀명: A팀
- 본인 이름(작성자): 김광진 (KimKwangjin)
- 공통과제(우리 팀이 자동화한 반복 수작업): (작성 예정)
- 내가 맡은 부분: 팀 공용 Git 저장소 구성·공유 (주장 역할)
- 자유과제(있으면):

> **이 로그는 본인 것만 작성**합니다. 각자 자기 PC·계정으로 작업해 개인 로그를 남기고, 제출 시 **영문 파일명** `<팀영문명>_<이름로마자>_PROCESS_LOG.md`(예: `teamA_kim_PROCESS_LOG.md`)로 저장하세요. **한글 파일명은 압축 시 깨지므로 금지** — 한글 팀명·이름은 위 '작성자 정보'에 적습니다. 운영자가 팀별로 모아 채점합니다(전원 참여 = 팀별 개인 로그 수).

## 효과 측정 (Before → After, 결과 ⑥ 채점용 — 형식 자유)
> **지표는 자기 업무에 맞게 고름 — 강제 항목 없음.** (예시, 해당되는 것만) 소요 시간 · 반복 횟수 · 다루는 자료/파일 수 · 손 가는 단계 수 · 품질·일관성 · 오류/누락 · 커버리지 등. 정량이 어려우면 정성도 인정.

| 지표(자기 업무에 맞게) | Before(기존 수작업) | After(에이전트화) |
|------|------|------|
|  |  |  |
|  |  |  |

## 사용 기법 (권장·가점, 필수 아님)
- [ ] (a) 서브에이전트 / 역할 분담
- [x] (b) 외부 도구·데이터 연동 (파일/API/MCP/사내데이터) — GitHub CLI(gh), Notion MCP
- [ ] (c) 재사용 산출물 (스킬 / 프롬프트셋 / CLAUDE.md / 서브에이전트 구성)

---

## 작업 로그 (단계마다 1개씩 누적 / 시간순)

### [#1] 팀 공용 Git 저장소 생성·공유 (Fork → 자동 구성)
- 작성자(팀원): 김광진 (KimKwangjin)
- 목표: A팀이 함께 쓸 Git 저장소를 만들고, 팀원이 클론·푸시로 협업하도록 공유. (Notion「Github 가이드」 PART A 흐름)
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "a팀의 git 저장소를 공유하고 싶어. 어떻게 해면 되는지 확인해보고 계획 보여줘"
  > "알아서 구성해주는 거야? github id는 있어"
- 사용한 기법(있으면): (b) 외부 도구·데이터 연동 — GitHub CLI(`gh`)로 Fork·remote·인증 자동화, Notion MCP로 팀 가이드 확인
- 결과:
  - `gh` 2.95.0 무설치 설치(`~/.local/bin`, sudo 불가 우회), `gh auth login`으로 계정 `somakkj-kuna` 인증
  - 킷 `limitda83/geosr-hackathon-kit`을 Fork → 팀 저장소 **`somakkj-kuna/team-a`** 생성 (submit/·CLAUDE.md 포함)
  - 로컬 `origin`을 team-a로 교체, 공식 킷은 `upstream`으로 보존
  - `git pull`/`git push` 검증 통과(인증 정상) → 공유 주소 `https://github.com/somakkj-kuna/team-a.git`
- 막힘 → 해결:
  - `sudo apt` 비밀번호 입력 불가 → gh 바이너리를 `~/.local/bin`에 무설치 다운로드로 해결
  - 팀원 이메일로 계정 검색 0건(이메일 비공개) → API 초대 불가 확인, GitHub 아이디 확보 또는 웹 이메일 초대로 우회 예정

### [#2] ...
(필요한 만큼 계속 추가)

---

## 마무리 요약 (1~2줄)
- 가장 효과적이었던 에이전트 활용법:
- 다른 팀이 그대로 따라 하려면 필요한 것:
